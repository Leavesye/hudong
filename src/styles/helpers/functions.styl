vendor(prop, args)
  -webkit-{prop} args
  -moz-{prop} args
  {prop} args
border-radius()
  vendor('border-radius', arguments)
box-shadow()
  vendor('box-shadow', arguments)

/*
button
  vendor('border-radius', 200px)*/

/* btns */
btn()
  line-height 100%
  text-align center
main-btn()
  vendor('border-radius', 3px)
  height 70px
  line-height 70px
  background-color #ed6196
  color #fff
  font-weight bold
  font-size 30px
  border: none;
